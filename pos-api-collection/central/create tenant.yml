info:
  name: create tenant
  type: http
  seq: 2

http:
  method: POST
  url: "{{base_url}}/central/tenants"
  body:
    type: json
    data: |-
      {
          "name": "test",
          "email": "test@example.com",
          "phone": "9801234568"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
  forwardAuthorizationHeader: true
